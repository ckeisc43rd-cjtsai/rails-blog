<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= mathjax_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7558522343211081" crossorigin="anonymous"></script>
  </head>
  <body class="bg-gray-900 text-gray-100 font-sans min-h-screen flex flex-col">
    <!-- Header / Navigation -->
    <header>
      <nav class="bg-gradient-to-r from-blue-900 to-blue-700 p-6">
        <div class="container mx-auto flex items-center justify-between">
          <h1 class="text-white text-2xl font-bold">
            <%= link_to "cjtsai's Blog", root_path %>
          </h1>
          <div class="flex space-x-4">
            <% if user_signed_in? %>
              <%= link_to current_user.username, edit_user_registration_path, class: "text-white hover:text-gray-300" %>
              <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-white hover:text-gray-300" %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "text-white hover:text-gray-300" %>
              <%= link_to "SignUp", new_user_registration_path, class: "text-white hover:text-gray-300" %>
            <% end %>
            <%= link_to "TOI", user_blog_posts_url(44), class: "text-white hover:text-gray-300" %>

          </div>
        </div>
      </nav>
    </header>

    <!-- Main Content -->
    <main class="flex-grow container mx-auto p-6">
      <% if notice.present? %>
        <div class="bg-green-800 text-green-200 p-3 rounded mb-4">
          <%= notice %>
        </div>
      <% end %>
      <% if alert.present? %>
        <div class="bg-red-800 text-red-200 p-3 rounded mb-4">
          <%= alert %>
        </div>
      <% end %>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-gray-300 py-4">
      <div class="container mx-auto text-center">
        <p>&copy; <%= Time.current.year %> cjtsai's Blog. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
