<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# react_component ‘MaterialComponentList’ %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%#= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%#= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="prose mx-auto h-screen  bg-blue-100 font-mono">    <nav class="flex items-center justify-between flex-wrap bg-blue-300 p-6">
    <div class="flex items-center flex-shrink-0 text-white mr-6">
        <h1><%=link_to "cjtsai's Blog", root_path %></h1>
    </div>
          <% if user_signed_in? %> 
            <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">              
              <h3><%=button_to "Logout", destroy_user_session_path, method: :delete %></h3>
            </div>
            <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">              
              <h3><%=link_to "Profile", edit_user_registration_path%></h3>
            </div>
          <% else %>
            <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">              
              <h3><%=link_to "Login", new_user_session_path%></h3>
            </div>
            <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">      
              <h3><%=link_to "SignUp", new_user_registration_path%></h3>
            </div>
          <% end %>        
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <%= yield %>
    <script src=”https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity=”sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL” crossorigin=”anonymous”></script>
  </body>
</html>
